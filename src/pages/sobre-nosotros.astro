---
import BaseLayout from '../layouts/BaseLayout.astro';
import MissionVision from '../components/about/MissionVision.astro';
import AboutUs from '../components/about/AboutUs.astro';
import AboutJosephPilates from '../components/about/AboutJosephPilates.astro';
import FounderProfile from '../components/about/FounderProfile.astro';
import CTAButton from '../components/etapa1/shared/CTAButton.astro';

const title = 'Sobre Nosotros - VivePilates';
const description =
  'Conoce nuestra misión, visión y valores. Somos una comunidad de profesionales certificados dedicados al bienestar a través del método Pilates en Vitacura.';
const currentPage = 'sobre-nosotros';
---

<BaseLayout title={title} description={description} currentPage={currentPage}>
  <!-- Hero inspiracional con cita de Joseph Pilates -->
  <section
    class="py-20 bg-gradient-to-br from-[var(--cream)] to-[var(--rose-pale)] text-center relative overflow-hidden hero-section"
  >
    <!-- Fondo animado con elementos abstractos -->
    <div class="absolute inset-0 overflow-hidden">
      <!-- Ondas de fondo -->
      <div class="wave-animation wave-1"></div>
      <div class="wave-animation wave-2"></div>
      <div class="wave-animation wave-3"></div>

      <!-- Partículas flotantes geométricas -->
      <div class="floating-elements">
        <div class="floating-circle circle-1"></div>
        <div class="floating-circle circle-2"></div>
        <div class="floating-circle circle-3"></div>
        <div class="floating-square square-1"></div>
        <div class="floating-square square-2"></div>
        <div class="floating-triangle triangle-1"></div>
        <div class="floating-triangle triangle-2"></div>
      </div>

      <!-- Gradiente dinámico superpuesto -->
      <div class="dynamic-gradient"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <h1
        class="text-4xl md:text-5xl font-bold mb-6 text-[var(--deep-coral)] leading-tight animate-fade-in-up"
      >
        "La contrología es la coordinación completa de cuerpo, mente y espíritu"
      </h1>
      <p
        class="text-xl md:text-2xl mt-4 text-[var(--trust-blue)] font-medium animate-fade-in-up animation-delay-300"
      >
        - Joseph Pilates
      </p>
      <p
        class="text-lg mt-6 text-[var(--charcoal-gray)] max-w-2xl mx-auto animate-fade-in-up animation-delay-600"
      >
        Descubre nuestra historia, misión y la filosofía que nos guía en
        VivePilates
      </p>
    </div>
  </section>

  <!-- Componentes principales -->
  <MissionVision />
  <AboutUs />
  <FounderProfile />
  <AboutJosephPilates />

  <!-- CTA final para conversión -->
  <section class="py-16 bg-[var(--warm-gray)] text-center relative">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl md:text-4xl font-bold mb-6 text-[var(--deep-coral)]">
        Únete a Nuestra Comunidad
      </h2>
      <p class="text-lg mb-8 text-[var(--charcoal-gray)] max-w-2xl mx-auto">
        Comienza tu viaje hacia el bienestar con profesionales certificados que
        entienden tus necesidades
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <CTAButton
          href="/servicios#clase-prueba"
          variant="primary"
          size="lg"
          icon="sparkles"
          tracking="about_page_cta_trial"
        >
          Agenda tu Clase de Prueba Gratuita
        </CTAButton>

        <CTAButton
          href="/servicios"
          variant="outline"
          size="lg"
          icon="arrow-right"
        >
          Ver Nuestros Servicios
        </CTAButton>
      </div>

      <!-- Badge de confianza adicional -->
      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <span
          class="bg-[var(--sage-green)] text-white px-4 py-2 rounded-full text-sm font-medium"
        >
          🌿 15 años de experiencia
        </span>
        <span
          class="bg-[var(--trust-blue)] text-white px-4 py-2 rounded-full text-sm font-medium"
        >
          ✨ Profesionales certificados
        </span>
        <span
          class="bg-[var(--warm-gold)] text-white px-4 py-2 rounded-full text-sm font-medium"
        >
          🏆 Método original de Joseph Pilates
        </span>
      </div>
    </div>
  </section>
</BaseLayout>

<!-- Schema markup para SEO (LocalBusiness) -->
<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "@id": "https://vivepilates.cl",
    "name": "VivePilates",
    "description": "Estudio de Pilates en Vitacura con enfoque en bienestar personalizado y método original de Joseph Pilates.",
    "url": "https://vivepilates.cl",
    "telephone": "+56 9 1234 5678",
    "email": "info@vivepilates.cl",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Av. Kennedy 6800",
      "addressLocality": "Vitacura",
      "addressRegion": "Región Metropolitana",
      "addressCountry": "CL"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "-33.38",
      "longitude": "-70.55"
    },
    "openingHoursSpecification": [
      {
        "@type": "OpeningHoursSpecification",
        "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
        "opens": "08:00",
        "closes": "20:00"
      },
      {
        "@type": "OpeningHoursSpecification",
        "dayOfWeek": "Saturday",
        "opens": "09:00",
        "closes": "14:00"
      }
    ],
    "sameAs": ["https://instagram.com/vivepilates"],
    "founder": {
      "@type": "Person",
      "name": "Joseph Pilates",
      "description": "Creador del método Pilates"
    },
    "serviceType": "Pilates Studio",
    "areaServed": "Vitacura, Las Condes, Providencia",
    "specialty": ["Pilates", "Rehabilitación", "Bienestar", "Fitness"]
  }
</script>

<style>
  /* ===== ANIMACIONES AVANZADAS PARA EL HERO ===== */

  /* Hero section base */
  .hero-section {
    position: relative;
    background: linear-gradient(
      135deg,
      var(--cream) 0%,
      var(--rose-pale) 50%,
      var(--cream) 100%
    );
  }

  /* Ondas de fondo abstractas */
  .wave-animation {
    position: absolute;
    border-radius: 50%;
    opacity: 0.1;
    filter: blur(1px);
  }

  .wave-1 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, var(--coral-primary), transparent 70%);
    top: -150px;
    left: -150px;
    animation: waveFloat1 20s ease-in-out infinite;
  }

  .wave-2 {
    width: 250px;
    height: 250px;
    background: radial-gradient(circle, var(--sage-green), transparent 70%);
    top: 50%;
    right: -125px;
    animation: waveFloat2 25s ease-in-out infinite reverse;
  }

  .wave-3 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--warm-gold), transparent 60%);
    bottom: -200px;
    left: 50%;
    transform: translateX(-50%);
    animation: waveFloat3 30s ease-in-out infinite;
  }

  @keyframes waveFloat1 {
    0%,
    100% {
      transform: translate(0, 0) rotate(0deg) scale(1);
    }
    25% {
      transform: translate(30px, -20px) rotate(90deg) scale(1.1);
    }
    50% {
      transform: translate(-20px, 30px) rotate(180deg) scale(0.9);
    }
    75% {
      transform: translate(20px, 10px) rotate(270deg) scale(1.05);
    }
  }

  @keyframes waveFloat2 {
    0%,
    100% {
      transform: translate(0, 0) rotate(0deg) scale(1);
    }
    33% {
      transform: translate(-25px, 25px) rotate(120deg) scale(1.15);
    }
    66% {
      transform: translate(15px, -15px) rotate(240deg) scale(0.85);
    }
  }

  @keyframes waveFloat3 {
    0%,
    100% {
      transform: translateX(-50%) translateY(0) rotate(0deg) scale(1);
    }
    50% {
      transform: translateX(-50%) translateY(-30px) rotate(180deg) scale(1.1);
    }
  }

  /* Partículas flotantes geométricas */
  .floating-elements {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .floating-circle,
  .floating-square,
  .floating-triangle {
    position: absolute;
    opacity: 0.15;
  }

  /* Círculos flotantes */
  .floating-circle {
    border-radius: 50%;
    background: linear-gradient(45deg, var(--coral-primary), var(--deep-coral));
  }

  .circle-1 {
    width: 60px;
    height: 60px;
    top: 20%;
    left: 15%;
    animation: gentleFloat 8s ease-in-out infinite;
  }

  .circle-2 {
    width: 40px;
    height: 40px;
    top: 60%;
    right: 20%;
    animation: gentleFloat 12s ease-in-out infinite reverse;
  }

  .circle-3 {
    width: 80px;
    height: 80px;
    bottom: 25%;
    left: 70%;
    animation: gentleFloat 10s ease-in-out infinite;
    animation-delay: -4s;
  }

  /* Cuadrados flotantes */
  .floating-square {
    background: linear-gradient(135deg, var(--sage-green), var(--vital-green));
    transform: rotate(45deg);
  }

  .square-1 {
    width: 30px;
    height: 30px;
    top: 40%;
    left: 80%;
    animation: rotateFloat 15s linear infinite;
  }

  .square-2 {
    width: 50px;
    height: 50px;
    top: 70%;
    left: 10%;
    animation: rotateFloat 20s linear infinite reverse;
  }

  /* Triángulos flotantes */
  .floating-triangle {
    width: 0;
    height: 0;
  }

  .triangle-1 {
    border-left: 25px solid transparent;
    border-right: 25px solid transparent;
    border-bottom: 43px solid var(--warm-gold);
    top: 30%;
    right: 15%;
    animation: triangleFloat 14s ease-in-out infinite;
  }

  .triangle-2 {
    border-left: 20px solid transparent;
    border-right: 20px solid transparent;
    border-bottom: 35px solid var(--trust-blue);
    bottom: 40%;
    right: 60%;
    animation: triangleFloat 18s ease-in-out infinite reverse;
  }

  /* Animaciones de elementos flotantes */
  @keyframes gentleFloat {
    0%,
    100% {
      transform: translateY(0px) translateX(0px) scale(1);
    }
    25% {
      transform: translateY(-15px) translateX(10px) scale(1.05);
    }
    50% {
      transform: translateY(-25px) translateX(-5px) scale(0.95);
    }
    75% {
      transform: translateY(-10px) translateX(-15px) scale(1.02);
    }
  }

  @keyframes rotateFloat {
    0% {
      transform: rotate(45deg) translateY(0px);
    }
    25% {
      transform: rotate(135deg) translateY(-20px);
    }
    50% {
      transform: rotate(225deg) translateY(-10px);
    }
    75% {
      transform: rotate(315deg) translateY(-30px);
    }
    100% {
      transform: rotate(405deg) translateY(0px);
    }
  }

  @keyframes triangleFloat {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
      opacity: 0.15;
    }
    50% {
      transform: translateY(-20px) rotate(180deg);
      opacity: 0.25;
    }
  }

  /* Gradiente dinámico superpuesto */
  .dynamic-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      45deg,
      transparent 0%,
      rgba(238, 126, 112, 0.03) 25%,
      transparent 50%,
      rgba(156, 175, 136, 0.03) 75%,
      transparent 100%
    );
    animation: gradientShift 12s ease-in-out infinite;
  }

  @keyframes gradientShift {
    0%,
    100% {
      opacity: 1;
      transform: translateX(0%);
    }
    50% {
      opacity: 0.7;
      transform: translateX(5%);
    }
  }

  /* Animaciones de entrada para el texto */
  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(30px) scale(0.95);
    }
    100% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }

  /* Mejoras de accesibilidad */
  @media (prefers-reduced-motion: reduce) {
    .wave-animation,
    .floating-circle,
    .floating-square,
    .floating-triangle,
    .dynamic-gradient {
      animation: none !important;
    }

    .animate-fade-in-up {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }
  }

  /* Optimización para dispositivos móviles */
  @media (max-width: 768px) {
    .floating-elements {
      opacity: 0.7;
    }

    .wave-animation {
      opacity: 0.08;
    }

    /* Reducir elementos en móvil para mejor rendimiento */
    .circle-3,
    .square-2,
    .triangle-2 {
      display: none;
    }
  }
</style>
